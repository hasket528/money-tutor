金博士 + 小金探 預錄語音存放處
────────────────────────────────
檔名依 voicegen/lists/*.csv 的 filename 欄，副檔名 .wav（或 .mp3）。
例：kb_a1_machine_fault.wav / detective_hint.wav
播放：chatbot-engine.js speak() 會先找 .wav → .mp3，找不到才用瀏覽器即時 TTS。
放檔即生效，不需改程式。
